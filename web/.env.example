# Copy to `.env.local` for local dev. On Vercel, set these in the project dashboard.
#
#   cp .env.example .env.local

# Fly.io agent API (default is production)
NEXT_PUBLIC_AGENT_BASE_URL=https://masterclass-first-agent.fly.dev
